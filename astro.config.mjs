// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
site: 'https://arranmilestones.co.uk',
// base: '/', // default is fine for a root custom domain
});
